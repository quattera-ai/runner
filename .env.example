# Copy to .env (next to docker-compose.yml). Never commit .env.
# One runner token per service — Settings → CI/CD → New runner (shown once).
AGENT_TOKEN_1=
AGENT_TOKEN_2=

# Optional: pin the image version (default: latest)
# QUATTERA_AGENT_TAG=2.0.92